import 'package:flutter/material.dart';

class AppConfig {
  static const String appName = 'Talent Spark';
  static const bool debugShowCheckedModeBanner = false;
  static const ThemeMode themeMode = ThemeMode.light;
}
